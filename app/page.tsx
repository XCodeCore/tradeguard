export default function Home() {
  return (
    <main className="mx-auto flex min-h-screen max-w-3xl items-center px-8">
      <section>
        <p className="text-sm uppercase tracking-widest text-zinc-500">Milestone 2</p>
        <h1 className="mt-3 text-4xl font-semibold">TradeGuard core engine</h1>
        <p className="mt-4 text-zinc-600">The normalized Binance MCP adapter and deterministic risk engine are ready. The polished dashboard is intentionally deferred.</p>
      </section>
    </main>
  );
}
